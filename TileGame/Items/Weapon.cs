namespace TileGame.Items
{
    class Weapon : ItemBase
    {
        public Weapon(string name, string description, double price, float weight, string textureName) : base(name, description, price, weight, textureName)
        {
        }
    }
}