namespace TileGame.Items
{
    class Armor : ItemBase
    {
        public Armor(string name, string description, double price, float weight, string textureName) : base(name, description, price, weight, textureName)
        {
        }
    }
}